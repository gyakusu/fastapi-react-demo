# FastAPI バックエンドの環境変数設定例
# ==========================================
# このファイルをコピーして .env ファイルを作成してください
# cp .env.example .env

# CORS設定: フロントエンドのオリジン（カンマ区切りで複数指定可能）
# 開発環境では "http://localhost:3000" を指定
# 本番環境では実際のフロントエンドのドメインを指定してください
CORS_ORIGINS=http://localhost:3000

# データベースURL（SQLite使用時）
# 例: sqlite:///./app.db
DATABASE_URL=sqlite:///./app.db

# JWT認証用のシークレットキー
# 本番環境では必ず強力なランダム文字列に変更してください
# 生成方法: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secret-key-here-change-in-production

# JWT トークンの有効期限（分）
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# API実行環境（development / production）
ENVIRONMENT=development
